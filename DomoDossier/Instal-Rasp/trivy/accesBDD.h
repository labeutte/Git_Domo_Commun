#ifndef ACCESBDD_H#define ACCESBDD_H#include <iostream>#include <fstream>#include <stdio.h>#include <stdlib.h>#include <unistd.h>#include <string>#include <time.h>#include <mysql/mysql.h>using namespace std;//static MYSQL *mysql; //Déclaration du pointeur sur MYSQLclass accesBDD {private:    string host;    string dbname;    string login;    string pwd;    int port;    string dt;//    static MYSQL *mysql; //Déclaration du pointeur sur MYSQL    MYSQL_ROW row;    MYSQL_FIELD *mfield;    MYSQL_RES *result;public:    accesBDD();     ~accesBDD();    string dateL();    string getnum(string); // récuperer tout les numéros d'un groupe donné -----------------    void updatetemp(unsigned long, float); // modifie la temperature d'un certain capteur ------------    void alertcapt(string); // ajoute un evenement dans la table alerte --------------------    void alertcam(string); // ajout un evenement dans la table alerte ----------------------    float gettemp(int); // recupere la temperature d'un certain capteur --------------------    string getidcapt(int); // récupere le ID d'un capteur en fonction de son code-----------    string getidcam(string); // récupere le ID d'une certaine caméra -----------------------    bool testpwd(string); // test la validité du mdp passé en parametre ---------------------    bool testpwdSMS(string); // test la validité du mdp SMS ---------------------    int getstate(); // recupere l'état du systeme ------------------------------------------    int getcle3G(); // recupere si il y a une cle 3G    float gettempalert(int); // récupere la temperature d'alerte d'un certain capteur ------    bool updatestate(int); // modifie l'état du systeme ------------------------------------    string getcamRep(string id); //récupère le repertoire d'accueil de la cam    int getNbCam(); // recupere le nombre de cameras    string getNomSite();    string getcamGeo(string id); //récupère le champ PosGeo    string getcaptname(int); // recupere le nom d'un capteur en fonction de son code -------    string getcamname(string);    string getftpadr();    string getftplogin();    string getftpmdp();    string getIndexImage();    string getLastImg(string numImg = "0");    void incIx();    void putLastImg(string);    bool coupure(); // Ajoute un evenement coupure de courant ------------------------------    bool retour(); // Ajoute un evenement retour du courant --------------------------------    void sortErr(int); //affiche le num d'erreur};#endif